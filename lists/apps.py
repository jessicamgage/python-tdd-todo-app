"""
This file stores metadata for Django. While minimalistic, it is a pwoerful tool that allows users to not have to manually update their INSTALLED_APPS via Django to make use of these configurations.
"""

from django.apps import AppConfig


class ListsConfig(AppConfig):
    name = 'lists'
